import React, { Component } from 'react';

class About extends Component {
    render() {
        //const nr = parseInt(this.props.match.params.number, 10);
        return <div>
            About
        </div>
    }
}

export default About;
